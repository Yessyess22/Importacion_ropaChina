# Tests de integración / E2E

Esta carpeta está reservada para pruebas de extremo a extremo (Playwright) que
ejerciten el sistema completo a través de Nginx.

Las pruebas unitarias de Django viven junto a cada app, en
`backend/apps/<dominio>/tests/`.

Se implementarán en una fase posterior.
