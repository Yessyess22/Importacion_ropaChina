# Bitácora de Desarrollo — Trendy Import SRL

Registro cronológico de hitos académicos. Cada entrada documenta qué se construyó, quién lo hizo y qué pruebas validaron el trabajo. El formato es inmutable: no se editan entradas pasadas, solo se agregan nuevas al final.

---

## Formato de registro

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|

---

## Fase 1 — Arquitectura Base

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-03-15 | Oscar | `docker-compose.yml`, `docker/`, `nginx/nginx.conf` | Estructura de contenedores Docker operativa (postgres, backend, frontend, nginx) | `docker compose up --build` sin errores; Nginx responde en `http://localhost/` |
| 2026-03-16 | Shirley | `backend/config/settings/`, `backend/manage.py` | Proyecto Django 5.2 inicializado y conectado a PostgreSQL 16 | `python manage.py check` sin errores |
| 2026-03-17 | Oscar | `frontend/`, `vite.config.ts`, `tailwind.config.*` | Proyecto React 19 + TypeScript + Vite + Tailwind CSS v4 configurado | `npm run dev` sin errores; página de bienvenida en `http://localhost/` |

---

## Fase 2 — Modelado e Integridad de Base de Datos

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-04-01 | Shirley | `apps/usuarios/models.py` | Modelos `Rol` y `Usuario` (AbstractUser) con FK protegida | `makemigrations` + `migrate` exitosos |
| 2026-04-03 | Oscar | `apps/terceros/models.py` | Clase abstracta `Tercero` + subclases `Proveedor`, `ClienteMayorista`, `AgenteAduanal`, `Transportista` | Test de integridad referencial: intentar borrar proveedor con importación activa → `PROTECT` funciona |
| 2026-04-05 | Shirley | `apps/catalogo/models.py` | Modelos `Prenda` + `VarianteProducto` con constraint único talla/color | Tests de unicidad de variante |
| 2026-04-08 | Oscar | `apps/importaciones/models.py` | `OperacionImportacion` + `DetalleImportacion` con valor_cif persistido | Migración aplicada; FK PROTECT sobre Proveedor/Agente/Transportista verificada |
| 2026-04-10 | Shirley | `apps/costeo/models.py`, `apps/documentos/models.py` | `Costeo`, `Tributo`, `TipoCambio`, `Documento` | Migración aplicada |
| 2026-04-12 | Oscar | `apps/inventario/models.py`, `apps/pedidos/models.py`, `apps/auditoria/models.py` | `MovimientoInventario`, `PedidoMayorista`, `DetallePedido`, `Bitacora` con GenericFK | Todas las migraciones aplicadas; `showmigrations` en verde |
| 2026-04-14 | Shirley | `apps/usuarios/management/commands/seed_dev_data.py` | Comando de semilla de datos (roles, admin, ejemplos) operativo | `python manage.py seed_dev_data` con `DJANGO_DEBUG=True` |

---

## Fase 3 — Autenticación y Control de Acceso

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-05-05 | Oscar | `apps/usuarios/authentication.py`, `apps/usuarios/views.py` | `LoginView`, `LogoutView`, `MeView` con SessionAuthentication + CSRF | Tests de login válido, login inválido, logout, 401 vs 403 |
| 2026-05-07 | Shirley | `apps/usuarios/permissions.py` | Fábrica `HasRole` + clase `Roles` con los 5 perfiles | Tests de denegación de acceso por rol en endpoints protegidos |
| 2026-05-09 | Oscar | `frontend/src/context/AuthContext.tsx`, `frontend/src/services/authService.ts` | Contexto global de sesión; `me()` en mount; login/logout reactivos | Prueba manual: login → `/` protegida; logout → redirige a `/login` |
| 2026-05-10 | Shirley | `frontend/src/components/ProtectedRoute.tsx`, `frontend/src/App.tsx` | Rutas protegidas por rol; `ModulePlaceholder` para módulos futuros | Prueba manual: rol Agente no accede a `/costeo`; redirige a `/` |
| 2026-05-11 | Oscar | `frontend/src/services/api.ts` | Cliente HTTP centralizado con `credentials: 'include'` y `X-CSRFToken` automático | Prueba manual: PATCH a endpoint protegido con cookie válida → 200 |

---

## Fase 4 — API REST de Negocio

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-06-02 | Shirley | `apps/terceros/views.py`, `apps/terceros/serializers.py` | CRUD completo de Proveedor, ClienteMayorista, AgenteAduanal, Transportista | `terceros/tests.py` (65 líneas) — todos en verde |
| 2026-06-04 | Oscar | `apps/catalogo/views.py`, `apps/catalogo/serializers.py` | CRUD de Prenda/Variante + acción `publicar` + filtro de catálogo mayorista | `catalogo/tests.py` (146 líneas) — todos en verde |
| 2026-06-08 | Shirley | `apps/importaciones/services.py`, `apps/importaciones/views.py` | CIF calculado en backend; máquina de estados de aduana; entrada de stock al liberar | `importaciones/tests.py` (145 líneas) — todos en verde |
| 2026-06-10 | Oscar | `apps/costeo/services.py`, `apps/costeo/views.py` | Cálculo de tributos y costeo total; `calcular-costeo` como acción explícita | `costeo/tests.py` (68 líneas) — todos en verde |
| 2026-06-12 | Shirley | `apps/inventario/services.py`, `apps/inventario/views.py` | `select_for_update` en `_aplicar_movimiento`; acciones `entrada/salida/ajuste` | `inventario/tests.py` (67 líneas) — stock negativo rechazado |
| 2026-06-15 | Oscar | `apps/pedidos/services.py`, `apps/pedidos/views.py` | Pedido con validación de mínimo por modelo + reserva atómica de stock | `pedidos/tests.py` (156 líneas) — todos en verde |
| 2026-06-17 | Shirley | `apps/documentos/views.py`, `apps/reportes/views.py` | CRUD de Documentos; reportes de importaciones/pedidos con filtros de fecha | `documentos/tests.py` (65 líneas), `reportes/tests.py` (25 líneas) |
| 2026-06-18 | Oscar | `apps/auditoria/services.py` | Servicio de bitácora activo; llamado desde importaciones y pedidos | Verificación manual: crear importación → registro en `Bitacora` |
| 2026-06-19 | Shirley | `docs/api.md`, `docs/authentication.md`, `docs/08-AUDITORIA_CODIGO.md` | **Cierre de Fase 4 — API REST estable y documentada** | Colección Postman ejecutada sin errores; OpenAPI sin warnings |

---

## Fase 5 — Frontend de Negocio y Pruebas E2E *(En curso)*

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-08-30 | Oscar | `docs/` (01 al 08), `docker-compose.yml`, `.env.example`, `.gitignore`, `README.md` | **Puesta a punto de infraestructura y gobernanza documental — Inicio de Fase 5** | Revisión de archivos creados; docker-compose validado con `docker compose config` |
| 2026-08-30 | Oscar | `frontend/src/components/ui/` (10 componentes shadcn/ui), `frontend/src/utils/formatters.ts`, `frontend/src/layouts/AuthLayout.tsx`, `frontend/src/layouts/AppLayout.tsx`, `frontend/src/pages/Login.tsx` (refactor), `frontend/src/App.tsx` (rutas anidadas) | **S1-T01 · S1-T02 · S1-T03 · S1-T04 completados:** instalación de componentes shadcn/ui (`table`, `dialog`, `input`, `label`, `select`, `badge`, `card`, `skeleton`, `sonner`, `alert-dialog`), `formatters.ts` en español/BOB, `AuthLayout` B2B de dos columnas, `AppLayout` con sidebar responsivo y nav filtrada por rol, refactor de `Login.tsx` con Sonner toast, enrutamiento anidado bajo `AppLayout` con `ProtectedRoute` doble capa | `tsc --noEmit` — 0 errores; `npm run lint` — 0 errores propios (3 warnings en código generado por shadcn) |
| 2026-08-30 | Oscar | `backend/apps/usuarios/serializers.py`, `backend/apps/usuarios/views.py`, `backend/apps/usuarios/urls_v1.py`, `backend/config/urls_v1.py`, `backend/apps/terceros/urls.py`, `frontend/src/services/api.ts`, `frontend/src/types/`, `frontend/src/pages/admin/Usuarios.tsx`, `frontend/src/pages/terceros/Proveedores.tsx`, `frontend/src/pages/terceros/ClientesMayoristas.tsx`, `frontend/src/pages/terceros/AgentesAduanales.tsx` | **S1-T05 · S1-T06 · S1-T07 · S1-T08 completados:** endpoint `/api/v1/usuarios/` (ReadOnly, Admin only), fix URL `/api/v1/clientes-mayoristas/`, mejora de extracción de errores DRF en `api.ts`, 4 vistas CRUD con tabla paginada, búsqueda, modal Dialog de alta/edición y AlertDialog de confirmación para toggle activo | `tsc --noEmit` — 0 errores |
| 2026-08-30 | Oscar | `backend/apps/auditoria/tests.py`, `backend/conftest.py`, `backend/pytest.ini`, `tests/auth.spec.ts`, `playwright.config.ts` | **S1-T09 · S1-T10 completados:** 7 tests unitarios Django para `auditoria.registrar()` (creación, SET_NULL, GenericFK) — 7/7 PASSED; 4 escenarios E2E Playwright (login, credenciales inválidas, logout con AlertDialog, protección de ruta por rol) | `pytest apps/auditoria/ -v` → 7 passed in 3.20s |
| 2026-08-30 | Oscar | `docker-compose.yml` | **Fix healthcheck backend:** reemplaza `curl` (no disponible en `python:3.12-slim`) por `urllib.request` de la stdlib de Python; agrega `start_period: 15s`. Todos los contenedores arrancan sin errores | `docker compose up -d` → 4/4 healthy |
| 2026-08-30 | Oscar | `.env`, `.env.example`, `frontend/src/services/authService.ts`, `frontend/src/layouts/AuthLayout.tsx` | **Fix login 404 y mejora UI de AuthLayout:** `VITE_API_URL` corregido a `/api` (sin `/v1`). `authService` desacoplado de `VITE_API_URL` — usa `AUTH_BASE='/api/auth'` para evitar doble-prefijo con endpoints no versionados. Panel izquierdo de login rediseñado: `bg-zinc-950` con orbes decorativos, texto hero, feature list y 4 stats. Login funcional — sesión activa verificada en `/` | Login exitoso con `admin / AdminDesarrolloUPDS2026!`; Dashboard visible con sidebar y rol correcto |
| 2026-08-30 | Oscar | `backend/apps/usuarios/serializers.py`, `backend/apps/usuarios/views.py`, `frontend/src/pages/admin/Usuarios.tsx` | **S1-T05 (refinamiento) — CRUD completo de Usuarios con UI premium:** `UsuarioWriteSerializer` con `password` estrictamente `write_only` y encriptado via `make_password`; `rol` como `SlugRelatedField(slug_field="nombre")` para lookup por nombre de rol. `UsuarioViewSet` elevado de `ReadOnlyModelViewSet` a `ModelViewSet` con `get_serializer_class()` que despacha el serializer correcto según acción. `Usuarios.tsx` rediseñado: modal `sm:max-w-150` con grid de 2 columnas (username/rol · nombre/apellido · email · contraseña), badges de rol con paleta pastel por perfil, `AlertDialog` de confirmación antes de cambiar `is_active`, spinner en submit y toasts Sonner para éxito/error | `tsc --noEmit` — 0 errores; flujo POST/PATCH verificado manualmente contra API |
| 2026-08-30 | Oscar/Agente | `frontend/src/pages/terceros/Proveedores.tsx` | **S1-T06 (rediseño premium) — Rediseño completo y CRUD Premium de Proveedores bajo la Ley de Consistencia Visual:** Action Bar Card con `p-6 shadow-primary/5` y botón `transition-all duration-200`; Interactive Table con `transition-colors hover:bg-secondary/40` en filas de datos y `bg-muted/50 font-semibold` en cabeceras; columna "Ciudad / País" combinada (`"Guangzhou, China"`); icono `Power` de lucide-react para toggle lógico de estado; Dialog Shell `sm:max-w-150` con grid 2 columnas (`razon_social`/`nit` · `fabrica`/`ciudad_origen` · `pais`/`telefono` · `email` full-width · `direccion` full-width); `ciudad_origen` marcado como requerido; `pais` inicializado en "China"; foco `ring-primary` en todos los inputs; AlertDialog con mensaje contextual de suspensión ("Esto afectará su visibilidad en nuevos registros"); toasts Sonner para éxito y errores DRF interceptados vía `extractErrorMessage` | `tsc --noEmit` — 0 errores; flujos POST/PATCH validados manualmente contra backend |
| 2026-08-30 | Oscar/Agente | `frontend/src/pages/terceros/ClientesMayoristas.tsx` | **S1-T07 (rediseño premium) — Rediseño completo y CRUD Premium de Clientes Mayoristas bajo la Ley de Consistencia Visual:** Action Bar Card `p-6 shadow-primary/5` con botón `transition-all duration-200` restringido a rol Administrador; Interactive Table con `hover:bg-secondary/40` y cabeceras `bg-muted/50 font-semibold`; columna Pedido Mínimo formateada como "Mín. X unid./modelo"; icono `Power` para toggle lógico; Dialog Shell `sm:max-w-150` con grid 2 columnas (`razon_social`/`nit` · `tipo_negocio` Select/`pedido_minimo_modelo` · `telefono` full-width · `email` full-width · `direccion` full-width); Select shadcn/ui para tipo de negocio (`Boutique`, `Tienda Física`, `Online`); payload sin `usuario`/`id`; foco `ring-primary`; AlertDialog con mensaje contextual de pedidos; toasts Sonner para éxito/error DRF | `tsc --noEmit` — 0 errores; flujos POST/PATCH validados manualmente contra backend |
| 2026-08-30 | Oscar/Agente | `frontend/src/pages/terceros/ClientesMayoristas.tsx` | **S1-T07 (rediseño premium) completado:** Reemplazo de `datalist` nativo por `Select` controlado de shadcn/ui. Formulario modal `Dialog` `sm:max-w-150` con grid de 2 columnas (Razón Social/NIT · Tipo de negocio/Pedido Mínimo · Teléfono full-width · Email full-width · Dirección full-width). Control de permisos restrictivo `canWrite` exclusivo para 'Administrador'. Badges pastel por estado (emerald/rose), hover en filas `hover:bg-secondary/40`, cabeceras `bg-muted/50 font-semibold`, inputs con foco unificado `ring-primary`, icono `Power` para toggle lógico de estado, AlertDialog contextual ("Esto afectará su visibilidad para registrar nuevos pedidos"), payload sin campo `usuario`, toasts Sonner para éxito/error DRF interceptados vía `extractErrorMessage`. | `tsc --noEmit` exitoso — 0 errores; validación manual de creación (POST) y edición (PATCH) en navegador. |
| 2026-08-30 | Oscar/Agente | `frontend/src/pages/terceros/AgentesAduanales.tsx` | **S1-T08 (rediseño premium) — Cierre de Sprint 1:** Consistencia visual alineada a la Ley de UI/UX. Action Bar Card `p-6 shadow-primary/5` con botón `transition-all duration-200`; `canWrite` corregido a Admin + Operador (se elimina Agente Aduanal del acceso de escritura). Interactive Table con `hover:bg-secondary/40` y cabeceras `bg-muted/50 font-semibold`; columna **Contacto** de dos líneas (Teléfono + Email). Dialog Shell `sm:max-w-150` con grid 2 columnas (Razón Social/NIT · Registro Aduanero/Teléfono · Email full-width requerido · Dirección full-width opcional); asteriscos `text-destructive` via `<span>`; foco `ring-primary` en todos los inputs; icono `Power` para toggle; AlertDialog contextual: "Esto afectará su visibilidad en el registro de nuevas operaciones de importación"; toasts Sonner para éxito/error. **Sprint 1 finalizado al 100%.** | `tsc --noEmit` — 0 errores; flujos POST/PATCH validados manualmente contra backend. |

---

## Fase 5 — Sprint 2: Operaciones de Importación, Documentos y Costeo *(En curso)*

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-08-30 | Shirley | `frontend/src/pages/importaciones/Importaciones.tsx`, `frontend/src/utils/importacionesUi.ts`, `frontend/src/utils/formatters.ts`, `frontend/src/App.tsx` | **S2-T01 completado — Vista `/importaciones`:** DataTable paginada consumiendo `GET /api/v1/importaciones/` con filtros reales de backend (`estado`, `proveedor`, `search` por código). Badge de color por estado, resolución de nombre de proveedor cruzando `GET /proveedores/`, botón "Nueva Importación" (Admin/Operador) enlazado a `/importaciones/nueva`. Se agregaron `REGISTRADA`/`LIBERADA` a `formatEstado`. Rutas placeholder `/importaciones/nueva` y `/importaciones/:id` registradas en `App.tsx` para no romper la navegación mientras se completan S2-T02 y S2-T03 | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores propios |
| 2026-08-30 | Shirley | `frontend/src/pages/importaciones/DetalleImportacion.tsx`, `frontend/src/types/terceros.ts`, `backend/apps/importaciones/serializers.py`, `frontend/src/App.tsx` | **S2-T03 completado — Vista `/importaciones/:id`:** detalle de la operación (proveedor, agente aduanal, transportista, FOB/flete/seguro/CIF), tabla de variantes ingresadas (talla/color/cantidad/costo unitario/subtotal), y modal de transición de estado con stepper visual del pipeline `REGISTRADA → EN_TRANSITO → EN_ADUANA → LIBERADA` + opción `CANCELADA`, con doble confirmación (`AlertDialog`) antes de invocar `POST /importaciones/{id}/actualizar-estado/`. **Fix de backend:** `DetalleImportacionNestedSerializer` no incluía `variante_detalle`, por lo que el detalle de importación no mostraba talla/color de las líneas — se agregó el campo anidado (mismo patrón que `DetalleImportacionSerializer`). Se agregó el tipo `Transportista` faltante en `frontend/src/types/terceros.ts` | `tsc --noEmit` — 0 errores; `python manage.py test apps.importaciones apps.catalogo` → 21/21 OK; verificación manual E2E con Playwright (login → listado → filtros → detalle → transición REGISTRADA→EN_TRANSITO confirmada con toast) |
| 2026-08-30 | Shirley | `frontend/src/pages/costeo/Costeo.tsx`, `frontend/src/App.tsx`, `frontend/src/pages/importaciones/Importaciones.tsx` | **S2-T05 completado — Vista `/costeo`:** selector de operación de importación, tarjetas de resumen (código/estado/CIF/costo total nacionalizado), formulario de registro de tributos (`ARANCEL`/`IVA`, base imponible, porcentaje — `monto` siempre lo calcula el backend), tabla de tributos con eliminación confirmada por `AlertDialog`, banner de "costeo desactualizado" tras agregar/eliminar un tributo, y acción `POST /importaciones/{id}/calcular-costeo/` para (re)calcular el costo total. **Fix de UI encontrado durante pruebas:** el `Select` de shadcn/base-ui no renderiza el texto del ítem elegido por defecto (solo el `value` crudo) — corregido con `children` como función en `SelectValue`, tanto en `Costeo.tsx` como en los dos filtros de `Importaciones.tsx` (estado y proveedor) que tenían el mismo defecto | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores propios; `python manage.py test apps.costeo apps.importaciones` → 12/12 OK; verificación manual E2E con Playwright: seleccionar operación → calcular costeo (CIF sin tributos) → agregar tributo IVA 13% sobre 600 → recalcular → costo total 678,00 BOB correcto |
| 2026-08-30 | Shirley | `frontend/src/pages/costeo/TipoCambio.tsx`, `frontend/src/layouts/AppLayout.tsx`, `frontend/src/App.tsx` | **S2-T06 completado — Vista `/tipo-cambio`:** CRUD completo (crear/editar/eliminar) sobre `TipoCambio` (fecha única, valor USD/BOB). Se agregó el ítem de navegación "Tipo de Cambio" al sidebar y la ruta `/tipo-cambio` — **no existían en ningún archivo**, era una brecha total (backend y tipo TS ya listos desde Sprint 1, pero nunca expuestos en la UI) | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores propios; verificado en navegador: alta, listado y formato de fecha correctos |
| 2026-08-30 | Shirley | `backend/config/settings/base.py`, `nginx/nginx.conf`, `frontend/src/types/documentos.ts`, `frontend/src/pages/documentos/Documentos.tsx`, `frontend/src/App.tsx` | **S2-T04 completado — Vista `/documentos`:** selector de operación, formulario de adjunto multipart (tipo/nombre/fecha de emisión/archivo), tabla de documentos con descarga y eliminación (bloqueada en operaciones `LIBERADA`, replicando la regla del backend). **2 fixes de infraestructura críticos encontrados y corregidos, sin los cuales la subida de archivos era inutilizable:** (1) `MEDIA_ROOT`/`STATIC_ROOT` en `base.py` usaban `BASE_DIR.parent` en vez de `BASE_DIR`, escribiendo los archivos subidos fuera del volumen Docker compartido con Nginx (`/media` en la raíz del contenedor en vez de `/app/media`) — se perdían al no persistir y eran inaccesibles; (2) `nginx.conf` no tenía ningún `location /media/`, así que cualquier descarga caía en el catch-all que proxea al frontend y devolvía 404 — se agregó el `location` con `alias /app/media/` y `client_max_body_size 20M` | `tsc --noEmit` — 0 errores; `python manage.py test apps.documentos` → OK; verificado end-to-end: `curl -I http://localhost/media/.../archivo.pdf` → `200 OK` con `Content-Type: application/pdf` |

---

## Fase 5 — Sprint 3: Catálogo Mayorista y Pedidos B2B *(En curso)*

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-08-31 | Shirley/Agente | `frontend/src/pages/catalogo/Catalogo.tsx`, `frontend/src/utils/catalogoImagenes.ts`, `frontend/src/utils/catalogoUi.ts`, `frontend/src/App.tsx`, `backend/apps/catalogo/serializers.py`, `backend/apps/catalogo/views.py`, `backend/apps/catalogo/tests.py`, `backend/apps/usuarios/management/commands/seed_dev_data.py` | **S3-T01 completado — Vista `/catalogo`:** grid de prendas (cards con imagen, código, nombre, categoría, rango de precio y chips de talla/color con badge de estado/stock) con filtros de búsqueda, categoría, talla y color contra la API. Las imágenes son fotos de stock de ropa juvenil de Unsplash mapeadas por categoría (`catalogoImagenes.ts`) — el modelo `Prenda` no tiene campo de imagen; cada URL se verificó manualmente antes de usarse, con `onError` a un ícono de fallback si la carga falla. **3 fixes de backend encontrados durante la implementación, requeridos por los criterios de aceptación del sprint:** (1) `PrendaSerializer.variantes` usaba el related manager sin filtrar, así que un Cliente Mayorista veía variantes en `BORRADOR`/sin stock anidadas dentro de una `Prenda` visible — se cambió a `SerializerMethodField` que filtra por rol; (2) el queryset del mayorista no exigía `stock_disponible__gt=0` a nivel de `Prenda`, dejando ver modelos sin ninguna variante comprable; (3) filtrar por `talla`+`color` vía `filterset_fields` (dos `.filter()` encadenados sobre la relación inversa `variantes`) hacía JOINs independientes y matcheaba combinaciones cruzadas (ej. talla de una variante + color de otra) — se reemplazó por un filtro manual con un único `.filter(variantes__talla=..., variantes__color=...)`. Se amplió `seed_dev_data` con 4 prendas demo (Chaquetas/Pantalones/Poleras/Sudaderas) en distintos estados y stocks para poder probar filtros y vista mayorista con datos realistas | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores propios (mismo warning preexistente de `set-state-in-effect` que ya tiene `Importaciones.tsx`); `python manage.py test apps.catalogo` → 17/17 OK (4 tests nuevos); verificado contra la API real con `curl` autenticado como `cliente_test` (Cliente Mayorista): ve 5/5 prendas con solo variantes publicadas y con stock, filtro combinado `talla=M&color=Negro` devuelve exactamente la prenda correcta; las 8 URLs de imagen responden `200 image/jpeg`. **Pendiente:** no se pudo tomar captura de pantalla real (sin navegador headless disponible en este entorno) — verificación fue funcional (API + tipos + lint + tests), no visual |
| 2026-08-30 | Shirley | `frontend/src/pages/importaciones/NuevaImportacion.tsx`, `frontend/src/App.tsx`, `frontend/src/utils/formatters.ts`, `frontend/src/pages/documentos/Documentos.tsx`, `frontend/src/pages/costeo/Costeo.tsx`, `frontend/src/pages/importaciones/Importaciones.tsx` | **S2-T02 completado — Vista `/importaciones/nueva`:** formulario de datos generales (proveedor/agente/transportista/fecha/ruta/FOB/flete/seguro) con CIF previsualizado en el cliente **solo informativo** (nunca se envía en el payload; el backend recalcula el real), y tabla de líneas de detalle dinámica (variantes aplanadas desde `GET /prendas/`, sin repetir variante entre líneas). Al guardar: `POST /importaciones/` y luego un `POST /detalles-importacion/` por línea, con redirección al detalle. **3 bugs de UI reales encontrados y corregidos durante la verificación:** (1) todos los `Select` con `value={x \|\| undefined}` disparaban la advertencia de React "uncontrolled → controlled" — se unificó a `value={x}` siempre string en `Documentos.tsx`, `Costeo.tsx` y `NuevaImportacion.tsx`; (2) botones que renderizan `<a>`/`<Link>` vía `render` sin `nativeButton={false}` disparaban warning de accesibilidad de base-ui; (3) **`formatDate()` corría las fechas puras `YYYY-MM-DD` un día hacia atrás** en zonas horarias negativas (America/La_Paz, UTC-4) por interpretarlas como medianoche UTC — bug preexistente que afectaba `fecha_registro`, `fecha_emision` y `fecha` de tipo de cambio en toda la app, corregido forzando hora local | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores propios; verificado en navegador: FOB 1000 + flete 150 + seguro 25 → CIF previsualizado y CIF real ambos 1.175,00 BOB; fecha mostrada corregida de "29" a "30 de agosto de 2026" |
| 2026-08-30 | Shirley | `package.json` (nuevo, raíz), `backend/apps/usuarios/management/commands/seed_dev_data.py`, `tests/importaciones.spec.ts` | **S2-T07 completado — E2E del flujo de importación.** **Gap de infraestructura crítico encontrado:** no existía ningún `package.json` en la raíz del repo — `playwright.config.ts` y `tests/auth.spec.ts` (documentados como "PASSED" en sesiones previas) no podían ejecutarse desde una instalación limpia porque `@playwright/test` no estaba declarado como dependencia en ninguna parte. Se creó `package.json` en la raíz y se instaló. Al correr la suite real por primera vez, el escenario E4 de `auth.spec.ts` **falló**: `seed_dev_data` nunca creaba el usuario `cliente_test` que ese test asume — se agregó su creación (+ un `ClienteMayorista` vinculado) al comando de seed. Con ambos fixes, se escribió `tests/importaciones.spec.ts`: login → registrar importación con 1 línea de detalle → verificar CIF → 3 transiciones de estado con confirmación (`REGISTRADA→EN_TRANSITO→EN_ADUANA→LIBERADA`) → verificar que `LIBERADA` es terminal | `npx playwright test` (desde la raíz, stack Docker activo) → **5/5 PASSED** (4 de `auth.spec.ts` + 1 de `importaciones.spec.ts`); verificado manualmente en la base de datos que la liberación generó `MovimientoInventario` tipo `ENTRADA` (RF-09) || 2026-09-01 | Agente | `frontend/src/components/pedidos/VarianteSelector.tsx`, `frontend/src/pages/pedidos/NuevoPedido.tsx`, `frontend/src/App.tsx`, `frontend/src/pages/catalogo/Catalogo.tsx` | **S3-T02 · S3-T03 completados — `VarianteSelector` y vista `/pedidos/nuevo`:** `VarianteSelector` es una tarjeta reutilizable por variante (talla/color/precio) con badge de stock (verde ≥10, ámbar 1-9, rojo/agotado si 0 o `estado !== 'PUBLICADO'`), stepper de cantidad acotado a `stock_disponible` y botón "Agregar". `NuevoPedido` construye un carrito agrupado por modelo (`Prenda`): si el rol es Cliente Mayorista, el mínimo por modelo se obtiene de `GET /clientes-mayoristas/` (el backend ya devuelve solo el propio registro); si es Administrador/Operador, se agrega un `Select` de cliente activo con su `pedido_minimo_modelo` visible en cada opción. Antes de habilitar el envío, se valida en cliente que la suma de cantidades por modelo cumpla el mínimo del cliente correspondiente — si no, `toast.error` detallando qué modelo falta y bloquea el `POST /pedidos/`. Al confirmar con éxito: toast con el código de pedido generado, carrito vacío y redirección a `/catalogo` (no a `/pedidos` ni `/pedidos/:id` — esas vistas son S3-T04/S3-T05, aún no existen). Se agregó el botón "Nuevo Pedido" en la Action Bar de `Catalogo.tsx` (visible para Administrador/Operador/Cliente Mayorista) porque el ítem de sidebar "Pedidos" sigue apuntando al `ModulePlaceholder` hasta S3-T04. No se tocó nada de backend: `POST /pedidos/` y el filtrado de `ClienteMayoristaViewSet` ya soportaban todo lo necesario | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores nuevos (mismo warning preexistente `set-state-in-effect` de `Catalogo.tsx`); `python manage.py test apps.pedidos apps.catalogo` → 27/27 OK; verificado end-to-end en navegador con Playwright ad-hoc (no committeado, vía `playwright` ya instalado en `node_modules` de la raíz): login `cliente_test` → `/catalogo` → botón "Nuevo Pedido" → seleccionar modelo → agregar 1 unidad → "Confirmar Pedido" bloqueado con toast "cantidad mínima por modelo es 10... (1/10)" → completar a 10 unidades → confirmar → toast de éxito con código `PED-20260901-1976C2` → redirección a `/catalogo` confirmada, sin errores de consola distintos al 401 esperado de `/api/auth/me/` antes de iniciar sesión; login `admin` en `/pedidos/nuevo` → `Select` de cliente visible con ambos clientes de seed y su mínimo correcto. El pedido de prueba fue eliminado y el stock restaurado manualmente (`inventario.services.registrar_entrada` + borrado de `PedidoMayorista`/`Bitacora`) al finalizar la verificación |
| 2026-09-01 | Agente | `frontend/src/pages/pedidos/Pedidos.tsx`, `frontend/src/utils/pedidosUi.ts`, `frontend/src/App.tsx`, `frontend/src/utils/formatters.ts` | **S3-T04 completado — Vista `/pedidos`:** DataTable paginada consumiendo `GET /api/v1/pedidos/` (el backend ya filtra al Cliente Mayorista a sus propios pedidos, sin necesidad de lógica adicional en el frontend) con filtro de estado (`ESTADOS_PEDIDO`/`ESTADO_PEDIDO_COLOR` en `pedidosUi.ts`, mismo patrón que `importacionesUi.ts`) y, solo para Administrador/Operador/Contabilidad, filtro y columna de cliente (nombre resuelto cruzando `GET /clientes-mayoristas/`, igual que la resolución de proveedor en `Importaciones.tsx`). La columna "Total" se calcula en cliente sumando `cantidad × precio_unitario` de los `detalles` anidados que ya devuelve el serializer de lista, sin peticiones extra. Botón "Nuevo Pedido" (Administrador/Operador/Cliente Mayorista) y "Ver detalle" por fila hacia `/pedidos/:id`, registrada como ruta placeholder (mismo patrón que S2-T01 con `/importaciones/:id`) para no romper la navegación hasta S3-T05. **Fix real encontrado durante la verificación:** `formatEstado()` en `formatters.ts` no tenía entradas para `ENVIADO`/`ENTREGADO`/`CANCELADO` (solo existían `ENTREGADA`/`CANCELADA` de otros módulos) — el *fallback* de capitalización solo pone en mayúscula la primera letra sin tocar el resto, así que un estado ya en mayúsculas como `"ENVIADO"` se mostraba igual, sin convertir a "Enviado"; se agregaron las 3 claves faltantes | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores nuevos (mismo warning preexistente `set-state-in-effect`); verificado end-to-end en navegador con Playwright ad-hoc (no committeado): Cliente Mayorista ve su pedido sin columna "Cliente" con el total correcto (480,00 BOB); Administrador ve la misma lista con la columna "Cliente" resuelta a "Cliente Demo Mayorista", filtro de estados muestra las 6 opciones con etiquetas correctas tras el fix, y "Ver detalle" navega a `/pedidos/2` (placeholder). Pedido de prueba eliminado y stock restaurado manualmente al finalizar, igual que en la verificación de S3-T02/S3-T03 |
| 2026-09-01 | Agente | `frontend/src/pages/pedidos/PedidoDetalle.tsx`, `frontend/src/utils/pedidosUi.ts`, `frontend/src/App.tsx` | **S3-T05 completado — Vista `/pedidos/:id`:** detalle del pedido (cliente resuelto vía `GET /clientes-mayoristas/{id}/`, fecha, total calculado de los `detalles` anidados) y tabla de líneas (talla/color/cantidad/precio unitario/subtotal). Modal de cambio de estado con stepper visual del pipeline lineal `PENDIENTE → CONFIRMADO → EN_PREPARACION → ENVIADO → ENTREGADO` + rama lateral `CANCELADO` (disponible solo hasta `EN_PREPARACION`, igual que en el backend), con doble confirmación (`AlertDialog`) antes de invocar `POST /pedidos/{id}/actualizar-estado/` — mismo patrón exacto que `DetalleImportacion.tsx` de Sprint 2, ya que `PEDIDO_TRANSICIONES_VALIDAS` del backend resultó ser estructuralmente igual (un pipeline lineal + una rama de cancelación condicionada al estado actual), así que se agregaron `PIPELINE_LINEAL_PEDIDO`/`TRANSICIONES_VALIDAS_PEDIDO` a `pedidosUi.ts` reflejando exactamente `backend/apps/pedidos/services.py`. El botón "Cambiar Estado" solo se muestra a Administrador/Operador (igual que el backend en `actualizar_estado`); Cliente Mayorista y Contabilidad ven la vista en solo lectura. Reemplazado el placeholder de `/pedidos/:id` en `App.tsx` por el componente real | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores nuevos (mismo warning preexistente `set-state-in-effect`); `python manage.py test apps.pedidos apps.catalogo` → 27/27 OK; verificado end-to-end en navegador con Playwright ad-hoc (no committeado): pedido creado como `cliente_test` → su vista de detalle no muestra "Cambiar Estado" → como `admin`, mismo pedido avanzado `PENDIENTE→CONFIRMADO→EN_PREPARACION` (cada transición con toast) → cancelado desde `EN_PREPARACION` (toast "Cancelado") → tras cancelar, el botón "Cambiar Estado" desaparece (estado terminal, `transicionesDisponibles.length === 0`). Pedido de prueba eliminado al finalizar (el stock ya había sido liberado automáticamente por el backend al cancelar) |
| 2026-09-01 | Agente | `frontend/src/pages/inventario/Stock.tsx`, `frontend/src/types/inventario.ts`, `frontend/src/utils/catalogoUi.ts`, `frontend/src/components/pedidos/VarianteSelector.tsx`, `frontend/src/utils/formatters.ts`, `frontend/src/App.tsx`, `frontend/src/layouts/AppLayout.tsx` | **S3-T06 completado — Vista `/stock`:** tabla paginada de variantes (`GET /variantes/`) con modelo (resuelto cruzando `GET /prendas/`, mismo patrón de resolución que proveedor/cliente en otras vistas), talla/color, estado (`ESTADO_VARIANTE_COLOR`) y stock con badge por umbral; filtro de búsqueda y de estado. Botón "Ver movimientos" por fila abre un modal con el ledger completo de esa variante (`GET /movimientos-inventario/?variante=<id>`): tipo (Entrada/Salida/Ajuste con color), cantidad con signo, fecha/hora y observación. Sin formulario de alta manual de movimientos — el criterio de aceptación del sprint solo pide consulta, no gestión manual de stock. Se extrajo `stockBadgeClasses()` de `VarianteSelector.tsx` (S3-T02) a `catalogoUi.ts` para no duplicar el mismo criterio de umbral en dos archivos. **Corrección de permisos real, encontrada al revisar el backend antes de implementar:** la ruta `/stock` y el ítem de sidebar eran un placeholder que permitía los 5 roles, pero `MovimientoInventarioViewSet` en el backend nunca permitió lectura a Cliente Mayorista (`LECTURA_ROLES` = Administrador/Operador/Contabilidad/Agente Aduanal) — se corrigieron ambos (`App.tsx`, `AppLayout.tsx`) para que coincidan, confirmado explícitamente con el usuario antes del cambio. **Fix de formato encontrado durante la verificación:** mismo patrón de bug que en S3-T04 — `formatEstado()` no tenía entradas para `ENTRADA`/`SALIDA`/`AJUSTE` ni para `BORRADOR`/`PUBLICADO`/`DESCONTINUADO` (el *fallback* de capitalización no funciona sobre strings ya en mayúsculas); se agregaron las 6 claves faltantes a `ESTADO_MAP` | `tsc --noEmit` — 0 errores; `oxlint` — 0 errores nuevos (mismos 2 warnings preexistentes `set-state-in-effect`); verificado end-to-end en navegador con Playwright ad-hoc (no committeado): Administrador ve 13 variantes con badges de estado/stock correctos, modal de movimientos de una variante muestra su historial completo (8 movimientos, entradas/salidas de las verificaciones previas de S3-T02 a S3-T05, todas con observación legible), filtro por estado "Publicado" reduce a 9 resultados; Cliente Mayorista no ve "Stock" en el sidebar y, al navegar directamente a `/stock`, `ProtectedRoute` lo redirige a `/`. No se creó ni modificó ningún dato durante esta verificación (solo lectura) |
| 2026-09-01 | Agente | `tests/pedidos.spec.ts` | **S3-T07 completado — E2E del flujo de pedido (cierre de Sprint 3):** login como `cliente_test` → `/catalogo` → botón "Nuevo Pedido" → seleccionar el modelo demo `DEV-VC-001` (única variante publicada con stock en el seed: L/Rojo, stock 40, para no depender de selectores ambiguos) → agregar 1 unidad → "Confirmar Pedido" bloqueado con toast que menciona la cantidad mínima (10, `pedido_minimo_modelo` fijo de `cliente_test` en el seed) → corregir sumando 9 más (total 10) → confirmar → toast de éxito con el código `PED-...` extraído por regex → redirección a `/catalogo` → verificación en `/pedidos` de que la fila con ese código muestra estado "Pendiente". **Bug real encontrado al escribir el test:** `page.getByRole('link', { name: /nuevo pedido/i })` nunca resuelve — el botón "Nuevo Pedido" (igual que "Nueva Importación" en Sprint 2) renderiza un `<a href>` vía la prop `render` de base-ui pero con `role="button"` explícito, no `role="link"`; se corrigió el selector del test a `getByRole('button', ...)` | `npx playwright test tests/pedidos.spec.ts` → 1/1 PASSED; suite completa `npx playwright test` (desde la raíz, stack Docker activo) → **6/6 PASSED** (4 de `auth.spec.ts` + 1 de `importaciones.spec.ts` + 1 de `pedidos.spec.ts`), sin regresiones. Se ejecutó `seed_dev_data` antes de correr la suite para asegurar niveles de stock deterministas (el comando resetea `stock_disponible` del catálogo demo en cada corrida). El pedido de prueba creado por este test **no se eliminó** de la base de desarrollo, siguiendo la misma convención que `importaciones.spec.ts` (los datos que deja la suite E2E no se limpian entre corridas; sí se limpiaron, en cambio, todos los pedidos creados durante la verificación manual ad-hoc de S3-T02 a S3-T06) |

---

## Fase 5 — Sprint 4: Reportes, Bitácora y Suite E2E Completa *(Cerrado — Fase 5 completa)*

| Fecha | Autor | Componente / Archivo(s) | Hito alcanzado | Pruebas ejecutadas |
|-------|-------|-------------------------|----------------|--------------------|
| 2026-09-02 | Agente | `frontend/src/pages/reportes/Reportes.tsx`, `frontend/src/components/charts/BarChart.tsx`, `frontend/src/utils/csv.ts`, `frontend/src/types/reportes.ts`, `frontend/src/App.tsx`, `frontend/src/layouts/AppLayout.tsx` | **S4-T01 · S4-T02 completados — Vista `/reportes`:** dos secciones independientes con su propia fila de filtros — "Importaciones por Estado" (rango de fechas `fecha_desde`/`fecha_hasta` contra `GET /reportes/importaciones/`, dos gráficos de barras: cantidad de operaciones y valor CIF total) y "Pedidos por Estado" (`Select` de cliente contra `GET /reportes/pedidos/?cliente=`). `BarChart.tsx` es un componente SVG propio (sin nueva dependencia de terceros): barras con tope redondeado de 4px, base cuadrada, valor y categoría siempre visibles como texto directo (nunca solo en tooltip), un único tono `fill-chart-1` (token de `index.css`) porque la categoría ya la identifica la etiqueta del eje — no se necesita un color por barra. Cada sección tiene tabla con badges reutilizando `ESTADO_IMPORTACION_COLOR`/`ESTADO_PEDIDO_COLOR` ya existentes, y botón "Exportar CSV" (`downloadCsv()` en `utils/csv.ts`, genera el archivo enteramente en cliente a partir de los datos ya cargados). **Fix de consistencia real (GAP-15):** `REPORTES_ROLES` del backend nunca incluyó a `Agente Aduanal`, pero `App.tsx`/`AppLayout.tsx` sí lo dejaban entrar a `/reportes` — quedaba oculto porque antes era un `ModulePlaceholder` sin llamadas a la API; se quitó de `allowedRoles` en ambos archivos | `tsc --noEmit` — 0 errores; verificado en navegador con Playwright ad-hoc: gráficos renderizan con las barras y valores esperados contra los 3 registros de importación y 3 de pedidos del seed |
| 2026-09-02 | Agente | `backend/apps/auditoria/serializers.py`, `backend/apps/auditoria/views.py`, `backend/apps/auditoria/urls.py`, `backend/config/urls_v1.py`, `frontend/src/pages/auditoria/Auditoria.tsx`, `frontend/src/types/auditoria.ts`, `frontend/src/App.tsx` | **S4-T03 completado — Vista `/auditoria`:** construido desde cero el endpoint `GET /api/v1/bitacora/` — no existía ningún serializer/view/url para `Bitacora`, solo el modelo (Fase 4) y el servicio `registrar()` (Sprint 1). `BitacoraViewSet` es `ReadOnlyModelViewSet` (la única vía de escritura sigue siendo `auditoria_services.registrar()` desde los servicios de dominio), `permission_classes=[IsAuthenticated, HasRole(Roles.ADMINISTRADOR)]`, `filterset_fields=["usuario"]` + `search_fields=["accion"]`. `BitacoraSerializer` expone `entidad_tipo` (nombre de modelo vía `SerializerMethodField`) en vez del `id` interno de `ContentType`. Frontend: tabla paginada con `Input` de búsqueda por acción, `Select` de usuario (poblado desde `GET /usuarios/?page_size=100`) y un `Dialog` con el JSON de `detalle` formateado (`<pre>`) para las filas que lo tienen | `tsc --noEmit` — 0 errores; verificado en navegador con Playwright ad-hoc: bitácora lista 37 registros del seed/sesiones previas, filtro por usuario y por texto de acción funcionan, diálogo de detalle muestra el JSON correcto |
| 2026-09-02 | Agente | `tests/reportes.spec.ts` | **S4-T04 completado — E2E del flujo de reportes:** 5 escenarios — reporte de importaciones (gráfico visible por `aria-label`, exportación CSV con nombre de archivo verificado vía evento `download`, filtro de fechas sin resultados muestra el estado vacío correcto), reporte de pedidos (exportación CSV), Cliente Mayorista bloqueado en `/reportes` (redirigido a `/` y sin el ítem en el sidebar), bitácora (listado no vacío, filtro por acción con resultado y sin resultado), Cliente Mayorista bloqueado en `/auditoria` | `npx playwright test tests/reportes.spec.ts` → 5/5 PASSED |
| 2026-09-02 | Agente | `tests/importaciones.spec.ts` | **S4-T05 completado — E2E de regresión del flujo de importación:** nuevo escenario E2 que cubre la rama de cancelación (`REGISTRADA → CANCELADA`), la única transición que el flujo feliz de S2-T07 (E1) no ejercitaba — confirma que `CANCELADA` es terminal (sin botón "Cambiar Estado") y que el filtro de búsqueda por código de `/importaciones` (S2-T01) sigue mostrando la operación con su estado actualizado | `npx playwright test tests/importaciones.spec.ts` → 2/2 PASSED |
| 2026-09-02 | Agente | `backend/requirements.txt`, `backend/apps/reportes/tests.py`, `backend/apps/auditoria/tests.py`, `backend/apps/terceros/tests.py` | **S4-T06 completado — Cobertura de tests backend:** se agregaron `pytest==8.*`, `pytest-django==4.*`, `coverage==7.*` a `requirements.txt` y se reconstruyó la imagen del backend, cerrando `GAP-9` (el ejecutable no existía en el contenedor pese a estar documentado como funcional desde Sprint 1). `apps/reportes/tests.py` ampliado de 25 líneas a 3 clases / 9 tests (agregación `Count`/`Sum` por estado, filtros de fecha y de cliente), cerrando `GAP-5`. `apps/auditoria/tests.py` con 5 tests nuevos (`BitacoraApiTests`) para el endpoint de S4-T03. **Causa raíz de `GAP-10` encontrada:** el test de `terceros` fallaba con 404 porque pegaba a `/api/v1/clientes/`, una URL que nunca existió (el router registra `clientes-mayoristas`) — no era un bug de la API, se corrigió la URL en el test | `coverage run manage.py test apps` → **85/85 tests PASSED**; `coverage report` → **94% de cobertura total** (supera el 85% objetivo de Sprint 4) |
| 2026-09-02 | Agente | `backend/pyproject.toml` (nuevo), 48 archivos de `backend/` reformateados, `frontend/src/App.tsx` (limpieza de `ModulePlaceholder` muerto) | **S4-T07 completado — QA final (cierre de Fase 5):** **`GAP-14` encontrado y resuelto** — `ruff` está documentado como gate obligatorio desde `08-CONTROL_SESION.md` (Sprint 1) pero nunca estuvo instalado; al agregarlo y correrlo por primera vez sin ningún `pyproject.toml`, reportó 170 errores (152 `RUF012`, un falso positivo masivo sobre los `Meta.fields = [...]` idiomáticos de DRF) y 48 archivos nunca formateados. Se creó `backend/pyproject.toml` fijando `select = ["E", "F", "W", "I"]` explícitamente en vez de depender de defaults no documentados, se corrigieron los 7 errores reales restantes (imports) y se corrió `ruff format .` una sola vez sobre todo el backend (cambios mecánicos de espaciado, sin alterar lógica) | `ruff check .` → 0 errores; `ruff format --check .` → 107 archivos conformes; `tsc --noEmit` → 0 errores; `oxlint` → 0 errores (mismos warnings preexistentes `set-state-in-effect`); grep de invariantes (`style={{`, `window.alert/confirm/prompt`, `fetch(` fuera de `services/`) → sin coincidencias; `npm run build` → build de producción exitoso; `python manage.py migrate --check` → sin pendientes; `python manage.py check --deploy` → sin errores nuevos; `npx playwright test` (suite completa) → **12/12 PASSED** |

**Cierre de Fase 5:** con los 4 sprints completos, no queda ningún `ModulePlaceholder` en `App.tsx` — las 13 rutas de negocio del mapa de roles están implementadas, probadas y documentadas. `docs/00-CONTEXTO_PROYECTO.md` actualizado para reflejar la Fase 5 completa.
